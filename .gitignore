ai-academy/KickStarter_1-highlight.mp4
secrets.local.php
scheduling/config.local.php
scheduling/.config-cache
scheduling/.tokens-account*
scheduling/.bookings
scheduling/.claude/

# Claude Code session scratch — never track worktree temp dirs or nested junk
.claude/worktrees/
.claude/settings.local.json
**/.claude/worktrees/
**/TEMP/
.fonts/

# Polaris MCP session config — session-local, changes every run
.mcp.json

# Python byte-code caches
__pycache__/
*.pyc

# Local embedding model artifacts are downloaded on demand and should not be committed.
models/embeddings/
